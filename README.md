
  # Figma QRStore

  This is a code bundle for Figma QRStore. The original project is available at https://www.figma.com/design/Pyjvh99rFuqNSGPivpwtfp/Figma-QRStore.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  